# Direct API origin (local default). Leave empty + set API_INTERNAL_URL for same-origin
# under Next basePath /career-forge (labs / prod compose rewrite mode).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
API_INTERNAL_URL=http://localhost:8000
# Optional — set when testing production-like builds locally
# NEXT_PUBLIC_BUILD_SHA=abc1234
# NEXT_PUBLIC_BUILD_TIME=2026-05-27T12:00:00Z
